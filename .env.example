OPENAQ_API_KEY=your-openaq-api-key
GCP_PROJECT_ID=your-gcp-project-id
# Local dev: keep the key in project root (git-ignored)
GCP_SA_FILE=service-account.json
# CI/CD example (set in secret/env, not committed):
# GCP_SA_FILE=/secrets/gcp/service-account.json
